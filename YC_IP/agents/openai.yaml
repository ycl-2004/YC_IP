interface:
  display_name: "YC 个人 IP 配图"
  short_description: "用红发透明眼镜 YC 生成白底手绘中文配图；默认 Sample，并直接出图。"
  default_prompt: "Use $YC_IP to 直接生成一张 YC 风格图片。默认 Sample / Standard；先读取 style-gate，实际附带 canonical 07-noise-filter.png，完成内部构思后必须调用 image_gen。纯白底、一个 YC、一个连续手绘场景；不要只输出分析或生图 prompt。"
policy:
  allow_implicit_invocation: true
